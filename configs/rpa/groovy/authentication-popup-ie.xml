<?xml version="1.0" encoding="UTF-8"?>
<config xmlns="http://web-harvest.sourceforge.net/schema/1.0/config" scriptlang="groovy">
    <robotics-flow>
        <robot name="ie" driver="chrome" close-on-completion="true">
            <script><![CDATA[
             	
             	timeouts(10 * 1000)
                open('http://browserspy.dk/password-ok.php')
                 
                // Confirming a simple alert
				//$(By.linkText("password-ok.php")).click();    
                //sleep(2000)
                //switchTo().alert().sendKeys('sure')
                SendKeys('test')
                confirm()
                 
            ]]></script>
        </robot>
    </robotics-flow>
       
	<!-- Export values -->
	<export include-original-data="false">    
		<single-column name="status" value="${status.toString()}"/>
	</export>
	
</config>	