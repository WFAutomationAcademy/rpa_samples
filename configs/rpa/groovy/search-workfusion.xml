<?xml version="1.0" encoding="UTF-8"?>
<config xmlns="http://web-harvest.sourceforge.net/schema/1.0/config" scriptlang="groovy">
    <selenium-flow>
		<!-- Internet explorer driver -->
        <selenium name="seleniumDriver" browser="internet explorer" close-on-completion="false" start-in-private="true">
            <script><![CDATA[
        		import java.util.concurrent.TimeUnit;

                ieDriver = seleniumDriver.getWrappedObject();               
                ieDriver.manage().timeouts().implicitlyWait(1, TimeUnit.MINUTES).pageLoadTimeout(1, TimeUnit.MINUTES);
                
                ieDriver.get("http://www.google.com");
                
        		element = ieDriver.findElement(By.name("q"));
        		element.click();
        		element.sendKeys("WorkFusion");
        		element.submit();
        		
        		ieDriver.findElement(By.xpath("(//h3[@class='r']/a)[1]")).click();
      
               	println ieDriver.getTitle();
                
				TimeUnit.SECONDS.sleep(30);    
				 
				ieDriver.quit();   		
            ]]></script>
        </selenium>
    </selenium-flow>
​
	<!-- Export values to the output CSV file -->
    <export include-original-data="false">
    </export>
</config>